# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'CatchMe' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for CatchMe
  pod 'SDWebImage'
  pod 'Alamofire'
  pod 'SwiftyJSON'

  target 'CatchMeTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'CatchMeUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
